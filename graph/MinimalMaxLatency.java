package graph;

/**
 * TikTok VO (2024/03/18)
 *
 * Given a weighted, bidirectional graph [1,2,3] -> [2,3,1] with weights [4,5,3]
 * Divide the graph into at most K subgraphs by removing connections.
 * A max latency is the maximal weight in the graph.
 * Return the minimal max latency of all subgraphs.
 *  3
 * / \
 * 1-2
 */
class MinimalMaxLatency {
    public int minimalMaxLatency(int[] from, int[] to, int[] weight, int k) {
        return 0;
    }
}
